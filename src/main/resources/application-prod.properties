# Perfil prod — pensado para API no Render + PostgreSQL no Neon.
# No Render: SPRING_PROFILES_ACTIVE=prod e variáveis abaixo (Environment).
#
# Neon (Dashboard → Connection string → JDBC):
#   DATABASE_URL=jdbc:postgresql://ep-XXXX.region.aws.neon.tech/neondb?sslmode=require
#   DATABASE_USER=neondb_owner (ou o usuário exibido)
#   DATABASE_PASSWORD=<senha do projeto>
#
# Render injeta PORT automaticamente. Não defina PORT manualmente.
#
# Front em outro domínio (ex.: Netlify):
#   CORS_ALLOWED_ORIGIN_PATTERNS=https://seu-app.netlify.app

server.port=${PORT:8080}
server.forward-headers-strategy=framework

spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USER}
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Tier free: pool pequeno (Neon também limita conexões)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1

app.cors.allowed-origin-patterns=${CORS_ALLOWED_ORIGIN_PATTERNS:}
